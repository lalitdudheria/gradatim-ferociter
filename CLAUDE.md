# Project guidance

- Never push or deploy a change without previewing it first. Run it locally (e.g. `python3 server.py` and open http://localhost:8080) and get explicit confirmation from the user that it looks right before committing and pushing to GitHub.
